services:
  bot:
    build: .
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/badminton.sqlite3
      MINIAPP_HOST: 0.0.0.0
      MINIAPP_PORT: 8080
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - bot-data:/app/data
    restart: unless-stopped
volumes:
  bot-data:
